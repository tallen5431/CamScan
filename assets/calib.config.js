// Central configuration for CalibOverlay mobile behavior
window.CalibConfig = {
  defaults: {
    labelScale: 1.5,
    linePx: 3,
    hitTolPx: 24,
    snapPx: 15,
    maxZoom: 20,
    minZoom: 0.2
  }
};