# CamScan Dependencies
# Core web framework. The overlay modules are plain assets (no version-specific Dash
# APIs), so both Dash 2.x and 3.x work — kept as ranges to match tested environments.
dash>=2.14,<4
flask>=3.0,<4
werkzeug>=3.0,<4

# Image processing
opencv-python>=4.8
numpy>=1.24

# CAD export (optional but recommended for DXF export)
ezdxf>=1.3

# Note: Dash will automatically install:
# - plotly
# - dash-html-components
# - dash-core-components
# - dash-table
